import React from 'react';

import './game.css';

const Game = (props) => {
  return (
    <canvas
      width={window.innerWidth}
      height={window.innerHeight}
    >
      
    </canvas>
  );
}

export default Game;